import React from 'react'

function CartForm({getInfo, orderId}) {
    console.log(getInfo)
    console.log(orderId)
    return (
        <div>
            
        </div>
    )
}

export default CartForm
